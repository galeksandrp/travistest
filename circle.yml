dependencies:
  override:
    - echo $PUBLIC_KEY2 >> ~/.ssh/authorized_keys
    - echo $PUBLIC_KEY2 | sudo tee /root/.ssh/authorized_keys
test:
  override:
    - watch -n 540 echo 'LOL'
general:
  branches:
    only:
      - /circle-.*/
