version: 2
jobs:
  build:
    docker:
      - image: galeksandrp/travistest:docker-build-android-openjdk8
    steps:
      - checkout
      - run: TERM=xterm watch -n 540 echo 'LOL'
workflows:
  version: 2
  build_and_test:
    jobs:
      - build:
          filters:
            branches:
              only: /circle-.*/
